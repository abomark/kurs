# Kopier denne til secrets.toml og fyll inn verdier.
# IKKE commit secrets.toml til git.

SUPABASE_URL = "https://<ditt-prosjekt>.supabase.co"
SUPABASE_ANON_KEY = "<anon-public-key>"
SUPABASE_SERVICE_KEY = "<service-role-key>"

ADMIN_PASSWORD = "bytt-meg"

# Valgfri: hvis satt, må deltakere skrive inn denne koden før de svarer.
# Sett til tom streng eller fjern linjen for å skru av.
PARTICIPANT_CODE = ""

<!-- UTKAST – Andre verifiserer -->
1. Åpne Cortex Code i Snowsight.
2. **Forstå skill-en først** — kjør:
   > _Hva gjør `@(serverSkill:lineage)`-skillen? Oppsummer når jeg skal bruke
   > den, hvilke input den forventer, og hvilke output den returnerer._
3. Les svaret: når passer skill-en, hvilke inputs trenger den, hvilke outputs
   returnerer den alltid?
4. **Anvend den** på en tabell du har tilgang til:
   > _Bruk `@(serverSkill:lineage)` på `<DIN.TABELL>` og vis meg opphav og
   > nedstrøms-avhengigheter._
5. (Valgfritt) Skru på Plan Mode (`Ctrl + P`) først hvis du anvender en skill
   som endrer en kjernetabell.
